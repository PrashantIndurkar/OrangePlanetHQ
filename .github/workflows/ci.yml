name: CI

on:
  push:
    branches:
      - main
      - master
      - dev
      - ci/github-actions
      - ci/gh-actions-automation
  pull_request:
    branches:
      - main
      - master
      - dev

jobs:
  test:
    name: Lint, Build & Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: stried_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@a51f1a4e21703657b98028f921588820c74ce924 # v4.0.0
        with:
          version: 11.5.3

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Cache pnpm store
        uses: actions/cache@1c6c59d9c8789d675661b016259062327a1c7263 # v4.2.2
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run Linting
        run: pnpm run lint

      - name: Generate Prisma Client
        run: pnpm --filter api exec prisma generate

      - name: Setup Database Schema
        run: pnpm --filter api exec prisma db push
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/stried_test?schema=public

      - name: Run Tests
        run: pnpm --filter api test -- --reporter=default --reporter=junit --outputFile.junit=test-results.xml
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/stried_test?schema=public
          JWT_SECRET: ci-test-secret-key-do-not-use-in-prod

      - name: Publish Test Report
        uses: dorny/test-reporter@883648a662da3f8fd8b491f85f1617182992df67 # v1.9.1
        if: always()
        with:
          name: Vitest Tests
          path: apps/api/test-results.xml
          reporter: jest-junit
